<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.csp.sentinel.dashboard.datasource.mapper.SentinelMetricsMapper">
    <select id="selectResourceByApp" resultType="java.lang.String">
        SELECT
            DISTINCT resource
        FROM
            sentinel_metrics
        WHERE
            app = #{app}
    </select>
</mapper>